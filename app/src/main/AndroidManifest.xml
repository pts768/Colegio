<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pablo.colegio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PantallaInicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfesorMain" />
        <activity android:name=".chattProfe" />
        <activity android:name=".infoProfe" />
        <activity android:name=".PadreMain" />
        <activity android:name=".infoPadre" />
        <activity android:name=".chattPadre" />
        <activity android:name=".bd.BDHelper" />
        <activity android:name=".calendarioPadre" />
        <activity android:name=".calendarioProfe"></activity>
    </application>

</manifest>
